<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.platform.dao.ApiGbLeaderMapper">

    <resultMap type="com.platform.entity.GbLeaderEntity" id="BaseResultMap">
        <result property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="realname" column="realname"/>
        <result property="mobile" column="mobile"/>
        <result property="pic" column="pic"/>
        <result property="registerTime" column="register_time"/>
        <result property="modifyTime" column="modify_time"/>
        <result property="status" column="status"/>
        <result property="remarks" column="remarks"/>
    </resultMap>

    <sql id="Base_Column_List" >
	    `id`,
	    `user_id`,
	    `realname`,
	    `mobile`,
	    `pic`,
	    `register_time`,
	    `modify_time`,
	    `status`,
	    `remarks`
  	</sql>


    <insert id="save" parameterType="com.platform.entity.GbLeaderVo" useGeneratedKeys="true" keyProperty="id">
		insert into gb_leader(
          `user_id`,
          `realname`,
          `mobile`,
          `pic`,
          `register_time`,
          `modify_time`,
          `status`,
          `remarks`        )
		values(
          #{userId},
          #{realname},
          #{mobile},
          #{pic},
          now(),
          now(),
          0,
          #{remarks}         )
	</insert>




    <select id="queryList" resultType="com.platform.entity.GbLeaderEntity">
        select
          `id`,
          `user_id`,
          `realname`,
          `mobile`,
          `pic`,
          `register_time`,
          `modify_time`,
          `status`,
          `remarks`
        from gb_leader
        WHERE 1=1
        <if test="name != null and name.trim() != ''">
            AND name LIKE concat('%',#{name},'%')
        </if>
        <choose>
            <when test="sidx != null and sidx.trim() != ''">
                order by ${sidx} ${order}
            </when>
            <otherwise>
                order by id desc
            </otherwise>
        </choose>
        <if test="offset != null and limit != null">
            limit #{offset}, #{limit}
        </if>
    </select>

    <select id="queryTotal" resultType="int">
        select count(*) from gb_leader
        WHERE 1=1
        <if test="name != null and name.trim() != ''">
            AND name LIKE concat('%',#{name},'%')
        </if>
    </select>
</mapper>